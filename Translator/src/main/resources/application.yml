spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bd_translator
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
#    hibernate:
#      ddl-auto: update  # создаёт таблицы автоматически, при необходимости
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true
        default_schema: core


